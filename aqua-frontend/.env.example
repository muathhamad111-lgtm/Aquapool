# Base URL of the aqua-app Laravel API. Read at build time by
# src/lib/api-client.ts — the build succeeds without it but produces a
# client that calls "undefined/api/v1/...", so it must always be set.
# Production: https://aqua-api.moathhamad.space
VITE_API_URL="http://localhost:8000"
