# docker

Reserved for container definitions (Laravel + PHP-FPM, PostgreSQL, Nginx)
once `aqua-app` is containerized for deployment. Empty for now — added in a
later phase per `docs/architecture.md`.
